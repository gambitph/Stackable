# Panel Design Library

A panel for picking designs from the block's design library.

# Usage

```
import PanelDesignLibrary from '@stackable/components'

const edit = props => {
	return (
		// ...
		<PanelDesignLibrary block={ props.name } />
		// ...
	)
}
```
